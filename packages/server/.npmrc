# Prevent sharp from downloading pre-built binaries
# Sharp is a transitive dependency from @xenova/transformers but we don't use it
# We only use Transformers.js for text embeddings, not image processing
sharp_binary_host=https://localhost/
sharp_libvips_binary_host=https://localhost/
sharp_libvips_version=false
